pluginManagement {
    repositories {
        maven { url = "https://maven.fabricmc.net/" }
        mavenCentral()
        gradlePluginPortal()
    }
}

// 注意：删除了之前导致报错的 dependencyResolutionManagement 块
rootProject.name = 'CTNHChangelog'